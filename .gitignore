# ── dependencies ─────────────────────────────────────────────
node_modules/
.venv/
venv/
__pycache__/
*.pyc

# ── secrets (never commit) ───────────────────────────────────
.env
.env.local
.env.*.local

# ── build output ─────────────────────────────────────────────
dist/
build/
.vite/

# ── huge raw datasets (200MB+ each — over GitHub's limit) ─────
# Download separately: MetroPT-3 from the UCI/Zenodo link,
# AI4I is small enough to stay in the repo.
ML server/MetroPT3(AirCompressor).csv
datasets/metropt+3+dataset/
datasets/metropt+3+dataset.zip

# ── large sequence caches only (20MB, unused at serve time;
# rebuild with rul_pipeline/01_build_dataset.py).
# The small X_/y_/t_ test arrays MUST stay tracked — /evaluate-rul
# loads them at runtime to draw the benchmark charts.
ML server/rul_pipeline/artifacts/seq_train.npy
ML server/rul_pipeline/artifacts/seq_test.npy

# ── OS / editor noise ────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
.gitignore.txt
